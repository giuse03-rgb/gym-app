syntax = "proto3";

package auth;

service AuthService {
  rpc Register (RegisterDto) returns (GenericResponseDto) {}
}

message RegisterDto {
  string username = 1;
  string password = 2;
}

message GenericResponseDto {
  string message = 1;
}
